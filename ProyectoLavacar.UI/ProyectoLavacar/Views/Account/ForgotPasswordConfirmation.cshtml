@{
    ViewBag.Title = "Confirmación de contraseña olvidada";
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title</title>
    <style>
        /* Estilos generales */
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        /* Contenedor principal */
        .container {
            text-align: center;
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            max-width: 400px;
        }

        /* Título */
        .title h1 {
            font-size: 24px;
            color: #333;
        }

        /* Caja del mensaje */
        .message-box {
            margin-top: 20px;
        }

            .message-box h2 {
                font-size: 22px;
                color: #007BFF;
            }

            .message-box p {
                font-size: 16px;
                color: #555;
            }

        /* Botón */
        .btn {
            display: inline-block;
            margin-top: 20px;
            padding: 10px 20px;
            text-decoration: none;
            color: white;
            background-color: #007BFF;
            border-radius: 5px;
            transition: background 0.3s ease;
        }

            .btn:hover {
                background-color: #0056b3;
            }
    </style>
</head>
<body>
    <main class="container">
        <hgroup class="title">
            <h1 id="title">@ViewBag.Title</h1>
        </hgroup>
        <div class="message-box">
            <h2>Solicitud enviada</h2>
            <p>Si el correo proporcionado está registrado en nuestro sistema, recibirás un mensaje con instrucciones para restablecer tu contraseña.</p>
            <a href="/" class="btn">Volver al inicio</a>
        </div>
    </main>
</body>
</html>
